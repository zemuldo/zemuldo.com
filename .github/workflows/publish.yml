name: Publish GRC

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build and Push
      uses: pangzineng/Github-Action-One-Click-Docker@master
    