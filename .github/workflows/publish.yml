name: Publish GRC

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v1
    - name: Setup Docker
      uses: pangzineng/Github-Action-One-Click-Docker